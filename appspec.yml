version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
    overwrite: true
hooks:
  AfterInstall:
    - location: scripts/start_server.sh
      timeout: 30
      runas: root
    - location: scripts/check_server_health.sh
      timeout: 30
      runas: root

